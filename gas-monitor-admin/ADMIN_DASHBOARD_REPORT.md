# Admin Dashboard Report — gas-monitor-admin

> Generated by Admin Dashboard OS, Phase 12. Date: 2026-09-03.

**Result: PASS with notes.** Typecheck clean, production build green, 13 routes.
Two items need your attention before deploy — see *Deploy prerequisites*.

---

## Summary

The admin was rebuilt on a configuration-driven resource engine: one table, one
filter bar, one detail drawer, one set of data states, used by every module. The
seven page files that each re-implemented fetching, tab filtering, and
client-side aggregation are now 16-line route files mounting a config.

Two correctness problems were fixed rather than restyled:

1. **The dashboard was displaying wrong numbers.** Revenue and counts on the
   overview, orders, vendors, listings and customers pages were computed in the
   browser from a `take: 200` capped fetch and labelled as platform totals. Every
   figure now comes from a server-side aggregate.
2. **SUPPORT admins could mutate platform data.** `requireAdmin` did not
   distinguish SUPPORT from OPERATIONS, so a support account could approve
   vendors, delete users and toggle listings by calling the endpoints directly.
   The UI never offered it, which is UX, not security.

Both the backend and the schema were changed, per your decisions. Nothing in the
consumer or vendor API surface was touched.

## What was built

| | Count |
|---|---|
| Modules generated | 9 |
| Routes | 13 (3 new: `/dashboard/analytics`, `/dashboard/audit`, `/dashboard/admin-users`) |
| Admin files added | 26 (`admin/` engine, 3,842 lines) |
| Admin files deleted | 22 (~1,700 lines of dead and duplicated code) |
| Backend files added | 4 |
| Backend files modified | 8 |
| **Dependencies added** | **0** |
| Dependencies removed | 6 |

## Modules

| Module | Entity | Mode | Route |
|---|---|---|---|
| vendors | `VendorProfile` | reduced | `/dashboard/vendors` |
| orders | `Order` | reduced (read-only) | `/dashboard/orders` |
| listings | `GasListing` | reduced | `/dashboard/listings` |
| users | `User` | full | `/dashboard/users` |
| customers | `User role=CONSUMER` | reduced (read-only) | `/dashboard/customers` |
| analytics | aggregate endpoint | full | `/dashboard/analytics` |
| audit | `AuditLog` **(new)** | read-only | `/dashboard/audit` |
| admin-users | `AdminUser` | full | `/dashboard/admin-users` |
| settings | `PlatformSettings` | full | `/dashboard/settings` |

### Degraded features

| Module | Feature | Why | What would enable it |
|---|---|---|---|
| orders | status transitions, cancel, refund | no write endpoint exists; order state is owned by the consumer/vendor flows and the Paystack webhook | a deliberate decision about what an admin override should do to payment state, then `PATCH /api/admin/orders/:id` |
| vendors | rejection reason | no field on `VendorProfile` | `rejectionReason String?` + surface it to the vendor |
| vendors | suspend | no suspension flag distinct from `REJECTED` | `suspendedAt DateTime?` |
| listings | create / edit / delete | vendors own their catalog in the vendor app; no admin write endpoint | a decision about whether admins should edit vendor catalogs at all |
| analytics | date range | `/analytics` has a hardcoded 6-month window | `?from=&to=` on the endpoint |
| users | create | endpoint exists and is wired in the data source, but no create form is built — Phase 6 prioritised the read/triage paths | a `FormConfig` for the users module |
| users | addresses, cylinder profiles, recent orders in the detail drawer | the drawer renders the **list row**, and those fields only come from `GET /api/admin/users/:id` | have `ResourceList` call `config.data.get(id)` on open and render the fetched record; `usersData.get` is already wired |
| vendors, listings, orders | same limitation | same — the drawer shows list-row fields only | as above. Vendor documents are unaffected: they are in the list `include`, so the drawer shows them today. |

### Skipped modules

| Module | Reason |
|---|---|
| payments, transactions, payouts | no `Payment`/`Transaction`/`Payout` entity. Paystack state lives on `Order` and is shown in the order detail drawer. |
| agents | no courier/rider entity |
| organizations | not multi-tenant |
| media | `VendorDocument` is a related list on the vendor, not a media library |
| content, moderation, notifications, reports | no corresponding entities |
| roles-permissions | `AdminRole` is a hardcoded Prisma enum, not a table — a permission matrix would have nothing to write to. The role model is surfaced **read-only** in Settings. |

`customers` was kept despite the usual rule that it should be skipped when
"customer" is only a role on `User`: the API exposes `totalSpend`, `orderCount`
and `addressCount`, which answer a different question from the Users directory.

## Permission map

| Permission | UI location | Server check | Status |
|---|---|---|---|
| `vendors.read` | Vendors nav + list | `requireAdmin` — `admin/vendors.ts:13` | present |
| `vendors.approve` | Approve/Reject actions | `requireOperations` — `admin/vendors.ts:80` | **added** |
| `orders.read` | Orders nav + list | `requireAdmin` — `admin/orders.ts:27` | present |
| `listings.read` | Listings nav + list | `requireAdmin` — `admin/listings.ts:21` | present |
| `listings.update` | Stock toggle | `requireOperations` — `admin/listings.ts:85` | **added** |
| `users.read` | Users nav + list | `requireAdmin` — `admin/users.ts:29` | present |
| `users.create` | (data source only) | `requireOperations` — `admin/users.ts:79` | **added** |
| `users.update` / `users.suspend` | Suspend / Reinstate | `requireOperations` — `admin/users.ts:171` | **added** |
| `users.delete` | Delete action | `requireOperations` — `admin/users.ts:243` | **added** |
| `customers.read` | Customers nav + list | `requireAdmin` — `admin/customers.ts:17` | present |
| `analytics.read` | Analytics page | `requireAdmin` — `admin/analytics.ts` | present |
| `audit.read` | Audit nav + list | `requireAdmin` — `admin/audit.ts:19` | **added** |
| `settings.read` | Settings page | `requireAdmin` — `admin/settings.ts:20` | **added** (was an inline null check) |
| `settings.update` | Settings save | `requireOperations` — `admin/settings.ts:38` | hardened (was an inline role comparison) |
| `admins.read/create/update/delete` | Admin users module | `requireSuperAdmin` — `admin/adminUsers.ts` | present |

**No `MISSING` rows.** Every permission the dashboard uses is now enforced
server-side. Frontend checks remain UX only.

### Role grants

| Role | Grants |
|---|---|
| SUPPORT | `*.read` — read-only across the whole panel |
| OPERATIONS | `*.read`, `vendors.approve`, `listings.update`, `users.*`, `settings.update` |
| SUPER_ADMIN | `*` |

The env root account (`ADMIN_USERNAME`/`ADMIN_PASSWORD`) still signs in as a
synthetic `SUPER_ADMIN` with `adminId: 'root'`, and is audit-logged under that id.

## Backend changes

**New**

- `prisma/schema.prisma` — `AuditLog` model + `AuditAction` enum
- `prisma/migrations/20260903000000_add_audit_log/` — hand-written, matching Prisma's format
- `src/lib/audit.ts` — `writeAuditLog()`; never throws, so an audit failure cannot roll back the operator's action
- `src/lib/listQuery.ts` — shared `?page&limit&q&sort&dir` parsing, sort-field whitelist, `{ data, pagination }` envelope
- `src/routes/admin/audit.ts` — read-only audit list + actor facets

**Modified**

- `src/middleware/requireAdmin.ts` — rank-based guards; added `requireOperations`
- `admin/{vendors,orders,listings,users,customers,adminUsers}.ts` — pagination, search, filters, sorting; guards and audit writes on mutations
- `admin/settings.ts` — inline role check replaced with `requireOperations`; audit write records the before→after diff
- `admin/adminUsers.ts` — added guards preventing self-deletion, self-deactivation, and removal of the last active super admin
- `src/app.ts` — mounted `/api/admin/audit`

**Response envelope changed** on all admin list endpoints, from `{ vendors }` /
`{ orders }` / … to `{ data, pagination }`. The admin panel is the only consumer
of `/api/admin/*`; the consumer and vendor routes under `/api/*` are untouched.

## Deploy prerequisites

Two things must happen, in this order:

1. **Run the migration.** The backend's `start` script already runs
   `prisma migrate deploy`, so deploying the backend applies
   `20260903000000_add_audit_log`. Until it runs, `/dashboard/audit` and every
   audit write will fail — the writes fail soft (logged, not thrown), the audit
   page shows its error state.
2. **Deploy the backend before the admin.** The admin now expects
   `{ data, pagination }` from the list endpoints. Deploying the admin first
   leaves every list empty.

## Post-deploy corrections (2026-09-03)

Two issues surfaced after the first deploy attempt.

**1. `Dockerfile` broke — my regression.** Removing `prisma/`, `@prisma/client`
and the `prisma` CLI left four dead lines in the Dockerfile:
`RUN npx prisma generate` and three `COPY` lines pointing at
`node_modules/.prisma`, `node_modules/@prisma/client` and `/app/prisma`. Any
Docker build would have failed. Fixed: those lines are gone, along with the
now-unneeded `openssl` install (it was only there for the Prisma query engine).

I checked source imports before deleting but not the build files. The validation
sweep should have grepped `Dockerfile` for references to deleted paths.

**2. Deployed admin calling `http://localhost:9000`.** Pre-existing, not caused by
this refactor — `lib/api.ts` is untouched and the previous login page used the
same `adminFetch`. `NEXT_PUBLIC_API_URL` is inlined at **build** time, and
`.env`/`.env.*` are excluded from both git and Docker, so any build that does not
receive the variable silently bakes in the localhost fallback.

Hardened rather than left as a footgun: `next.config.js` now throws during
`phase-production-build` when `NEXT_PUBLIC_API_URL` is unset, naming where to set
it for local, Netlify and Docker. Verified — a build without the variable fails
with that message; a normal build still passes and bakes in
`https://ugo.4fgmonitor.com`.

The check is scoped to the build phase only: at `next start` the value is already
in the bundle and is legitimately absent from the runtime environment.

## Round 2 — security and observability (2026-09-04)

Added after review: the OS registry had no `security`, `error-logs` or
`server-logs` modules at all. That was a gap in the skill, not a gas-monitor
decision, and it would have recurred on every future install. The skill now
carries all three (22 modules), every profile recommends security and
error-logs, and Phase 1 detection probes for trust-proxy, rate limiting, helmet
and error persistence.

**Built here — UI only, by decision.** Enforcement lands at the VPS stage.

| Route | Module | State |
|---|---|---|
| `/dashboard/security` | blocked addresses: list, filters, block/lift/remove, detail | **UI only — no backend** |
| `/dashboard/error-logs` | errors by category (incl. ATTACK) and severity, resolve/reopen, stack + redacted context | **UI only — no backend** |

`server-logs` was **skipped**: Render owns the logs and the filesystem is
ephemeral. There is nothing for the app to read. Logs live in the Render console.

### Both pages carry a standing notice

Each renders a permanent banner saying it is not connected. This is deliberate
and should not be removed until the backend exists — on the security page an
empty list must not imply protection, and on the error page an empty list must
not read as "no attacks".

### Backend work these need

**Security** — `TODO(admin-os)` in `admin/modules/security.tsx`:

```
GET    /api/admin/security/blocked-ips?page&limit&q&sort&dir&active&scope
POST   /api/admin/security/blocked-ips   { ipAddress, reason?, scope, expiresAt? }
PATCH  /api/admin/security/blocked-ips/:id   { active }
DELETE /api/admin/security/blocked-ips/:id
authz  read -> requireAdmin; block/unblock -> requireSuperAdmin
audit  every block and unblock writes an AuditLog entry
```

**BLOCKING PREREQUISITE — `app.set('trust proxy', 1)` is not set.** On Render,
`req.ip` is the load balancer's address. Wire enforcement before fixing that and
every record holds the same address, and the first block takes the API offline.
Fix trust-proxy first, always.

**Error logs** — `TODO(admin-os)` in `admin/modules/error-logs.tsx`:

```
GET   /api/admin/error-logs?page&limit&q&sort&dir&category&severity&resolved
PATCH /api/admin/error-logs/:id   { resolved }
authz read -> requireAdmin; resolve -> requireOperations
```

Nothing persists errors today: `captureException` sends to Sentry, and
`src/routes/internalSentryWebhook.ts` forwards issue alerts to Telegram without
storing them. The cheapest honest path to real data is extending that existing
webhook to also insert an `ErrorLog` row. **Do not query the Sentry API from the
browser** — it needs a privileged token.

### Permission grants

`security.block` and `security.unblock` are **SUPER_ADMIN only**, not OPERATIONS.
A bad block can take the platform offline, so it sits with the highest role.
`errors.resolve` is OPERATIONS.

### Security decisions worth knowing

- **Redaction.** `admin/primitives/redact.ts` masks credential-shaped keys and
  values (authorization, cookie, token, JWT-shaped strings) before request
  context is rendered. This is a last line of defence — **the server must redact
  at capture time**, or secrets sit in the database and in every export.
- **Stack traces and context are attacker-influenced text.** Rendered as escaped,
  pre-formatted content inside a disclosure, never as HTML.
- **Category colours map by meaning.** ATTACK and SYSTEM_RISK are error-toned;
  USER_ERROR is neutral, because a validation failure is not an incident.

### Not done, deliberately

**No pricing module.** GoBuyMe's is 2,327 lines because delivery pricing has
zones, surge windows and distance buckets. Gas-monitor's entire pricing model is
`platformFeePercent` on `PlatformSettings` and `pricePerKg` per listing — both
already surfaced in Settings and Listings. Building zones and surge here would be
inventing UI for data that does not exist. If gas-monitor should grow real
pricing rules, that is a product decision and a schema change first.

## Follow-ups not done

- **`gas-monitor-web/prisma/schema.prisma` was not updated** with the `AuditLog`
  model. Per the monorepo convention the two copies are mirrored by hand, but
  adding a model to web without a matching migration there risks a Prisma drift
  error, and web is outside this refactor's scope. It already lags the backend by
  `devicePhone` (pre-existing). Mirror both when convenient.
- **A users create/edit form.** `usersData.create` and `.update` are wired; the
  form config is not built.
- **`npm run lint` does not work** — see below.

## Verification

| Gate | Command | Result |
|---|---|---|
| Typecheck (admin) | `npx tsc --noEmit` | **pass**, clean |
| Build (admin) | `npm run build` | **pass** — 13 routes, compiled in 48s |
| Typecheck (backend) | `npx tsc -p tsconfig.json --noEmit` | **pass**, clean |
| Build (backend) | `npm run build` | **pass** |
| Lint | `npm run lint` | **pre-existing failure** |
| Tests | — | none exist in either project |

**Pre-existing lint failure.** `npm run lint` prompts interactively to configure
ESLint and cannot complete. No ESLint config has ever been committed to this
project (`git ls-files gas-monitor-admin | grep -i eslint` returns nothing),
so the script has never worked. Not introduced here, and not fixed here — adding
a config would surface an unknown number of pre-existing violations in files
outside this change. Say the word and I will add one and work through the fallout.

### Checks run

| Check | Result |
|---|---|
| Hardcoded colours outside `app/tokens.css` | none |
| Every token defined in light, dark, and system-dark blocks | 35/35 parity, no dark-only token |
| Mock or fixture data in shipped code | none |
| Duplicated list/pagination/debounce logic in modules | none — modules are pure config |
| `TODO(admin-os)` markers left | none |
| Module files over 150 lines | none |

### Responsive

Breakpoints implemented at 767 / 1279px. Sidebar becomes a focus-trapped
off-canvas drawer below 768px; tables become card lists driven by
`ColumnDef.priority`; stat grids collapse 4 → 2 → 1; inputs go to 16px to stop
iOS zoom; touch targets go to 40–44px. Table scroll is on the table wrapper, so
the page body never scrolls horizontally.

**Not verified in a real browser** — no dev server was started during this run.
The layout rules are implemented and the build is green, but if you want visual
confirmation at 375/768/1024/1440, run `npm run dev` and I will check it.

### Accessibility

Implemented: skip link; landmarks; one `<h1>` per page; visible 2px focus ring on
every interactive element (never removed); dialog and drawer focus trap with
Escape-to-close and focus return; body scroll lock; `<th scope>` with `aria-sort`
on sortable headers; row actions named with the record they act on; `aria-live`
on result counts and toasts; `role="alert"` on errors; status never encoded by
colour alone; `prefers-reduced-motion` honoured.

Contrast was designed to AA and the token values carry their ratios in comments —
`--warning` and `--error` were darkened from the originals (`#a9714c` → `#8a5a2f`,
`#b3413a` → `#a5322c`) to clear 4.5:1 on white. `--on-solid` flips to near-black
in dark mode, because white on the lighter dark-mode fills would have been ~2:1.

**Not verified with a screen reader or an automated axe pass.**

## How to add the next module

```
1. admin/modules/<name>.tsx     — a ResourceConfig; data only, no fetching
2. admin/core/navigation.config.ts  — add the nav entry
3. admin/permissions/permissions.ts — add the permission and grant it
4. app/dashboard/<name>/page.tsx    — 16 lines mounting <ResourceList config={...} />
```

Adding a module requires no change to the resource engine. If it does, add a
registered column or filter type instead.
